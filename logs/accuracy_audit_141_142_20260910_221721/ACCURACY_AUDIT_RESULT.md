# 141/142 高精度审计结果

## 结论

本轮审计已于 2026-09-10 22:54:39 完成，但没有产生可用于比较的 `key_phases.csv`。
六个组合全部在瞬态研究 `sol2/t1` 的第一个时间步失败，COMSOL 报告“找不到一致的初始值”。
因此，本轮不能证明也不能否定原始扫描中 z=141 全负、z=142 全正的物理差异。

原始 138--145 扫描结果必须继续标记为“数值准确性待验证”，不能把本轮失败当成 141/142 的物理结论。

## 实际执行矩阵

| 配置 | z=141 | z=142 | 实际设置 | 结果 |
|---|---|---|---|---|
| A0 | ERROR | ERROR | 原 solver tree；瞬态 `fc1 -> i1 -> gmres`，默认 `rtol=0.01` | 首步一致初值失败；141 日志还显示线性迭代发散，142 达到最大中间迭代次数 |
| A1 | ERROR | ERROR | 稳态/瞬态 `fc1` 实际改为 `dDef`，`dDef.linsolver=pardiso` | PARDISO 已生效，但仍在首个瞬态时间步报一致初值失败 |
| A2 | ERROR | ERROR | A1 设置 + 瞬态 `rtol=1e-6`；稳态 `rtol` 设置返回 `Unknown_property` | PARDISO 已生效，瞬态 `rtol=1e-6` 已写入，但仍未完成首步 |

## 关键证据

- 每个审计任务都成功完成几何和网格：
  - z=141：60997 个网格单元。
  - z=142：60847 个网格单元。
- 每个任务都成功完成稳态初始场 `sol1`，并保存了 `configured.mph`。
- 失败发生在 `sol2/t1`，不是几何、网格生成或结果读取阶段。
- A1/A2 的 stdout 明确记录：`fc1 linsolver=dDef`、`dDef linsolver=pardiso`。
- A2 的稳态相对容差设置不被当前稳态节点接受：`RTOL_SET_FAILED ... Unknown_property`；瞬态节点的 `rtol=1e-6` 设置成功。
- A0 z=141 的 progress 日志显示线性迭代发散；A0 z=142 显示达到最大中间迭代次数。

## 对原始结果的影响

原始结果中的 141/142 正负翻转目前不能作为已审计的物理现象。失败更像是瞬态初始化/首步求解路径问题，但仅凭这轮结果还不能排除时间依赖初始化、求解器设置或模型状态差异。

本轮没有修改力表达式、磁化方向、几何尺寸、z 定义或受力对象，也没有用失败点填零、插值或替换成其他尝试结果。

## 下一步建议

1. 先在 z=141 和 z=142 各自的 `configured.mph` 上单独打开 COMSOL 检查 `sol2/t1` 的初始值来源、初始时间步和旋转磁化在 t=0 的一致性。
2. 保持物理模型和网格不变，先做“静态固定姿态”的 0°/180° 对照，确认力计算本身可复现。
3. 若必须保留瞬态，使用已收敛的相同初始相位场，并逐步缩小首步时间/角度；每次只改一个数值设置。
4. 在修复首步一致初值前，不继续扩展 z 扫描，也不把原始 141--145 曲线解释为连续物理趋势。

## 文件位置

- 现场目录：`H:\comsolcc\accuracy_audit_141_142_20260910_221721`
- A0/A1/A2 每个 z 点的 `stdout.log`、`stderr.log`、`progress.log` 和 `configured.mph` 均已保留。
- 原始扫描目录未覆盖：`H:\comsolcc\fixed_field_refined_scan_138_145`
